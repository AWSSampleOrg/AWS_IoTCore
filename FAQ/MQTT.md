1. MQTT 5 (Clean Start = 1 and Session expiry interval = 0) = MQTT 3.1.1 (Clean Session = 1)
2. $share/{ShareName}/{TopicFilter}
3. AWS IoT Core requires an exact match string match between the subscription filter and the retained topic for immediate delivery.
